# Vite production env
VITE_NODE_ENV=production

# For same-origin IIS deployment, keep the API relative.
# This avoids mixed-domain and CORS issues in production.
VITE_API_URL=/api/v1

# Optional realtime endpoint if you add sockets later.
# VITE_SOCKET_URL=wss://pms.gitakshmi.com
